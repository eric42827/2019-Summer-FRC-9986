package frc.robot;

//import edu.wpi.first.wpilibj.Joystick;
import edu.wpi.first.wpilibj.XboxController;
//import edu.wpi.first.wpilibj.buttons.JoystickButton;

public class OI {
  
  public XboxController Controller = new XboxController(RobotMap.Joystick_Port);
  //public JoystickController Controller = new JoystickController(RobotMap.Joystick_Port);

  //public JoystickButton Button_Up = new JoystickButton(Controller, RobotMap.Up_Button);
  //public JoystickButton Button_Down = new JoystickButton(Controller, RobotMap.Down_Button);
  //public JoystickButton Button_Right = new JoystickButton(Controller, RobotMap.Right_Button);
  //public JoystickButton Button_Left = new JoystickButton(Controller, RobotMap.Left_Button);
  //public JoystickButton Button_LB = new JoystickButton(Controller, RobotMap.LB_Button);
  //public JoystickButton Button_RB = new JoystickButton(Controller, RobotMap.RB_Button);
  //public JoystickButton Button_Back = new JoystickButton(Controller, RobotMap.Back_Button);
  //public JoystickButton Button_Start = new JoystickButton(Controller, RobotMap.Start_Button);
  //public JoystickButton Button_JL = new JoystickButton(Controller, RobotMap.JL_Button);
  //public JoystickButton Button_JR = new JoystickButton(Controller, RobotMap.JR_Button);

  // Run the command while the button is being held down and interrupt it once
  // the button is released.
  // button.whileHeld(new ExampleCommand());

  // Start the command when the button is released and let it run the command
  // until it is finished as determined by it's isFinished method.
  // button.whenReleased(new ExampleCommand());
}
